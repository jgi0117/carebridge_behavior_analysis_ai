from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "Raw-data restructuring needs the original dataset layout under data/raw. "
        "The later preprocessing/window/train/API pipeline is structured and ready."
    )


if __name__ == "__main__":
    main()
